# Introduction Shortening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Shorten the dissertation introduction to about three PDF pages while preserving all required introduction sections.

**Architecture:** Keep the document structure unchanged and edit only the shared characteristic text in `common/characteristic.tex`. The dissertation wrapper in `Dissertation/introduction.tex` remains responsible for the chapter heading and automatic volume/statistics block. Verification is based on a full dissertation build and a page-range check in `dissertation.pdf`.

**Tech Stack:** LaTeX, XeLaTeX through the repository `Makefile`, BibLaTeX/Biber as configured by the template, command-line PDF text extraction with `pdfinfo` and `pdftotext`.

---

## Files

- Modify: `common/characteristic.tex` - compact introduction content shared through `\input{common/characteristic}`.
- Read-only: `Dissertation/introduction.tex` - confirms the wrapper and final volume/statistics paragraph.
- Verify: `dissertation.pdf` and LaTeX logs generated by `make dissertation`.
- Do not stage: `Dissertation/part1.tex` - this file is already dirty before this work and is unrelated to introduction shortening.

## Task 1: Replace the characteristic text with a compact draft

**Files:**
- Modify: `common/characteristic.tex:1-196`

- [ ] **Step 1: Confirm unrelated dirty work is present but unstaged**

Run:

```bash
git status --short
```

Expected: `Dissertation/part1.tex` may appear as modified. Do not stage or revert it.

- [ ] **Step 2: Replace the full contents of `common/characteristic.tex`**

Use this exact file content:

```latex
{\actuality} Текстовые документы остаются основным средством хранения
и~передачи информации в~деловой, учебной и~научной деятельности. Их объём
в~личных и~корпоративных хранилищах растёт быстрее, чем поддаётся ручному
упорядочению, поэтому актуальна автоматическая классификация документов
по~тематическим или функциональным категориям конкретного пользователя.

Электронная почта является характерным примером такой задачи. Почтовый ящик
профессионального пользователя объединяет переписку, уведомления, документы,
задачи и~технические сообщения объёмом до~нескольких сотен входящих писем
в~день \autocite{Singh2023EmFORE}. Ручная сортировка такой корреспонденции
требует постоянных временных затрат, а~правила по~отправителю или теме быстро
устаревают при смене проектов, контрагентов и~форматов уведомлений.

Готовые почтовые клиенты предлагают механизмы приоритизации входящих сообщений,
но чаще ориентируются на~общие категории вроде «важное~/ неважное» и~не
отражают индивидуальную структуру архива. При этом письма содержат персональные
данные, коммерческую информацию и~фрагменты внутренних документов, поэтому
передача текста во~внешние сервисы не~всегда допустима. Это делает востребованной
локальную систему, которая формирует персонализированные классы по~исторической
почте и~классифицирует новые сообщения без выхода текста за~контролируемый
контур.

{\progress} Задача автоматической классификации текстов исследуется в~рамках
обработки естественного языка и~машинного обучения: от~статистического
рубрицирования документов до~современных векторных представлений. Применительно
к~электронной почте известны подходы TaskTracer, где предсказываются проекты
пользователя с~помощью наивных байесовских и~линейных классификаторов
\autocite{Keiser2009TaskTracer}, и~EmFORE, где правила распределения писем
по~папкам обучаются онлайн с~учётом дрейфа пользовательских категорий
\autocite{Singh2023EmFORE}. Эти работы показывают необходимость учитывать
персональные классы, новые типы сообщений, низкоуверенные решения и~обратную
связь пользователя.

В~данной работе рассматривается локальный pipeline: нормализация писем,
построение multilingual-эмбеддингов, кластеризация исторической почты для
начального формирования классов и~обучение классификатора на~полученной
разметке. Низкоуверенные сообщения выделяются в~категорию \texttt{other} как
отказ от~автоматической классификации и~материал для последующей проверки.

{\aim} работы является разработка интеллектуальной системы классификации
текстовых документов на~примере электронной почты, способной работать локально,
поддерживать персонализированные классы, формировать их по~исторической выборке
и~оценивать качество принимаемых решений.

Для достижения поставленной цели необходимо решить следующие {\tasks}:
\begin{enumerate}[beginpenalty=10000]
  \item проанализировать подходы к~классификации текстовых документов
        и~автоматической организации электронной почты;
  \item сформулировать постановку задачи с~персонализированными классами,
        порогом уверенности и~явным отказом от~классификации;
  \item разработать локальную архитектуру обработки писем, включающую
        синхронизацию, нормализацию, векторизацию и~хранение артефактов;
  \item разработать способ начального формирования пользовательских классов
        кластеризацией исторической выборки с~выделением выбросов;
  \item реализовать классификатор и~решающее правило с~категорией
        \texttt{other} для низкоуверенных сообщений;
  \item провести экспериментальное исследование качества кластеризации,
        классификации и~операционной работы системы.
\end{enumerate}

\textbf{Объект исследования.} Процесс автоматической многоклассовой
классификации текстовых документов с~учётом персонализированных пользовательских
классов.

\textbf{Предмет исследования.} Методы и~алгоритмы построения локальной системы
классификации электронной почты, включая нормализацию текста, векторное
представление сообщений, кластеризацию исторической выборки, обучение
классификатора и~обработку отказов от~классификации.

{\methods} В~работе применяются методы системного анализа, обработки
естественного языка и~машинного обучения: построение векторных представлений,
кластеризация на~основе плотности, обучение классификаторов и~оценка качества
моделей. Для экспериментов используются accuracy, macro-F1, взвешенный F1,
precision, recall, показатели кластеризации, покрытие и~доля отказов
в~категорию \texttt{other}. Воспроизводимость обеспечивается фиксированной
конфигурацией, сохранением артефактов моделей и~версионированием данных.

{\novelty}
\begin{enumerate}[beginpenalty=10000]
  \item Предложен локальный pipeline классификации электронной почты, объединяющий
        нормализацию MIME-сообщений, multilingual-эмбеддинги, кластеризацию
        исторической выборки и~классификацию новых сообщений без передачи текста
        во~внешние сервисы.
  \item Разработан способ начального формирования пользовательских классов через
        кластеризацию исторической почты с~выделением выбросов и~последующей
        ручной проверкой кандидатов.
  \item Предложена схема принятия решений с~явным отказом от~классификации
        (\texttt{other}) и~quality gates для активации модели по~метрикам
        качества и~операционным характеристикам.
\end{enumerate}

{\influence} работы состоит в~создании MVP-прототипа локальной системы
классификации электронной почты для окружений, где требуется сохранить
приватность почтовых данных и~получить персонализированную автоматическую
классификацию. Прототип выполняет read-only IMAP-синхронизацию, нормализацию
писем, кластеризацию, обучение модели, инференс и~сохранение предсказаний без
изменения состояния почтового ящика.

{\defpositions}
\begin{enumerate}[beginpenalty=10000]
  \item Локальные multilingual-эмбеддинги применимы для построения единого
        признакового пространства русско-английских почтовых сообщений
        и~могут использоваться как основа кластеризации и~классификации.
  \item Кластеризация исторической почтовой выборки позволяет формировать
        кандидаты на~персонализированные классы без предварительного задания
        фиксированного числа категорий и~с~явным выделением выбросов.
  \item Порог уверенности, категория \texttt{other} и~quality gates снижают риск
        ошибочной автоматической классификации и~создают основу для управляемой
        обратной связи.
\end{enumerate}

{\reliability} результатов обеспечивается использованием открытых методов
машинного обучения и~обработки текста, воспроизводимой конфигурацией
экспериментов, сохранением артефактов моделей и~данных, сравнением с~базовыми
подходами, unit-тестами, статическим анализом и~контролем типов программной
реализации.

\textbf{Структура работы.} Работа состоит из~введения, трёх глав, заключения,
списка использованных источников и~приложений. Первая глава содержит анализ
задачи и~обзор подходов, вторая глава описывает архитектуру системы, третья
глава посвящена реализации и~экспериментальной оценке.
```

- [ ] **Step 3: Check the edit for trailing whitespace**

Run:

```bash
git diff --check -- common/characteristic.tex
```

Expected: no output and exit code `0`.

## Task 2: Build the dissertation and measure the introduction length

**Files:**
- Verify: `dissertation.pdf`
- Verify: LaTeX build logs produced by `make dissertation`

- [ ] **Step 1: Build the dissertation**

Run:

```bash
make dissertation
```

Expected: exit code `0`; `dissertation.pdf` is regenerated.

- [ ] **Step 2: Check for unresolved references and citations**

Run:

```bash
rg -n "undefined references|Citation .* undefined|There were undefined|Rerun to get cross-references right|LaTeX Warning: Reference .* undefined|LaTeX Warning: Citation .* undefined" dissertation.log
```

Expected: no output. If the only match is a rerun warning after a successful
first pass, run `make dissertation` once more and repeat this step.

- [ ] **Step 3: Locate the PDF pages where the introduction and chapter 1 start**

Run:

```bash
for p in $(seq 1 "$(pdfinfo dissertation.pdf | awk '/^Pages:/ {print $2}')"); do
    text="$(pdftotext -f "$p" -l "$p" dissertation.pdf -)"
    if printf '%s\n' "$text" | rg -q '^[[:space:]]*Введение[[:space:]]*$|^[[:space:]]*Глава 1[[:space:]]*$'; then
        printf '%s: ' "$p"
        printf '%s\n' "$text" | rg -m1 '^[[:space:]]*Введение[[:space:]]*$|^[[:space:]]*Глава 1[[:space:]]*$'
    fi
done
```

Expected: one line for `Введение` and one later line for `Глава 1`. The
introduction length is the `Глава 1` page number minus the `Введение` page
number. The accepted result is `2`, `3`, or `4`; the preferred result is `3`.

## Task 3: Apply the exact second-pass adjustment when the page count is outside the target range

**Files:**
- Modify: `common/characteristic.tex`

- [ ] **Step 1: If the introduction is longer than 4 pages, replace the structure block**

Replace the final block beginning with `\textbf{Структура работы.}` with:

```latex
\textbf{Структура работы.} Работа состоит из~введения, трёх глав, заключения,
списка использованных источников и~приложений. Главы последовательно раскрывают
анализ задачи, архитектуру системы, реализацию и~экспериментальную оценку.
```

- [ ] **Step 2: If the introduction is still longer than 4 pages, shorten the tasks list**

Replace the six-item tasks list with:

```latex
Для достижения поставленной цели необходимо решить следующие {\tasks}:
\begin{enumerate}[beginpenalty=10000]
  \item проанализировать подходы к~классификации текстов и~автоматической
        организации электронной почты;
  \item сформулировать постановку задачи с~персонализированными классами
        и~отказом от~классификации;
  \item разработать локальную архитектуру обработки, кластеризации
        и~классификации писем;
  \item реализовать прототип системы и~провести экспериментальную оценку
        качества её работы.
\end{enumerate}
```

- [ ] **Step 3: If the introduction is shorter than 2 pages, add one context paragraph**

Insert this paragraph immediately before `{\progress}`:

```latex
Дополнительную сложность создают смешанные русско- и~англоязычные сообщения,
длинные цепочки переписки, подписи, HTML-разметка и~технические уведомления.
Поэтому система должна не только выбирать классификатор, но и~обеспечивать
устойчивую предобработку текста, единый формат нормализованного сообщения
и~сопоставимые признаки для писем разных языков.
```

- [ ] **Step 4: Rebuild and remeasure**

Run:

```bash
make dissertation
```

Expected: exit code `0`.

Then repeat Task 2 Step 3. Expected: the introduction length is `2`, `3`, or
`4` pages; `3` remains preferred.

## Task 4: Final verification and commit

**Files:**
- Modify: `common/characteristic.tex`
- Do not stage: `Dissertation/part1.tex`

- [ ] **Step 1: Run the final whitespace check**

Run:

```bash
git diff --check -- common/characteristic.tex
```

Expected: no output and exit code `0`.

- [ ] **Step 2: Confirm only the introduction file is staged for this change**

Run:

```bash
git status --short
```

Expected: `common/characteristic.tex` is modified. `Dissertation/part1.tex` may
also be modified but remains unrelated.

- [ ] **Step 3: Stage only the introduction change**

Run:

```bash
git add common/characteristic.tex
git diff --cached --name-only
```

Expected:

```text
common/characteristic.tex
```

- [ ] **Step 4: Commit the introduction shortening**

Run:

```bash
git commit -m "Shorten dissertation introduction"
```

Expected: one commit containing only `common/characteristic.tex`.
