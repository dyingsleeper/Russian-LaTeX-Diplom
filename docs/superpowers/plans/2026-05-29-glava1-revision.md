# Ревизия главы 1 (анализ задачи и обзор) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Привести главу 1 диссертации к самодостаточному теоретическому виду, где `other` — это только отказ от классификации, терминология русифицирована, таблица сравнения согласована с текстом, а введение переписано под фактическое содержание глав.

**Architecture:** Правки сосредоточены в `Dissertation/part1.tex` (содержание главы) и `common/characteristic.tex` (введение, общее для диссертации и автореферата по ГОСТ). Глава 1 остаётся чисто теоретической: проектные решения (взвешивание потерь, переобучение, выбор конкретных моделей) исключаются или переводятся в анонс §1.7; отсылки «в главе 2/3» заменяются локальными выводами самой главы. Проверка — сборка `make dissertation-draft` без ошибок и без битых `\ref/\eqref`.

**Tech Stack:** XeLaTeX (BACKEND по умолчанию), latexmk через Makefile, biber/biblatex. Сборка из корня репозитория.

> **Решения заказчика, зафиксированные в плане:** (1) `other` — только отказ; (2) `p_k` → апостериорные оценки принадлежности; (3) `out-of-distribution`; (4) англицизмы → русский, оригинал в скобках при риске путаницы; (5) исправить таблицу; (6) кратко обосновать оценки; (7) `other` метриками не покрывается (OOD-метрику НЕ добавлять); (8) переписать введение под главы, а не главу под введение; (9) перефразировать двойную ссылку на формулу; (10) добавить абзац-вывод в §1.5; (11) мелкая унификация; (12) глава чисто теоретическая, без проектных решений; (13) глава самодостаточна; (14) мостик «тексты→почта» НЕ трогаем.

**Принцип проверки для prose-правок:** «тест» = успешная сборка + отсутствие новых warning о неразрешённых ссылках. Сборка LaTeX медленная, поэтому полная проверка делается на границах фаз (Task с пометкой BUILD), а не после каждого микроредактирования.

---

## Файлы

- Modify: `Dissertation/part1.tex` — глава 1 целиком (Фазы A–D)
- Modify: `common/characteristic.tex` — введение/характеристика работы (Фаза E)
- Verify: сборка `make dissertation-draft` из корня репозитория

---

## Фаза A. Формальная постановка (§1.2)

### Task A1: `other` как отказ — переписать §1.2.1 (формализм)

**Files:**
- Modify: `Dissertation/part1.tex:65-100`

- [ ] **Step 1: Заменить блок множества меток и постановки (строки 65-100).**

Заменить текст от «Пусть \( X \)…» до конца `eq:decision-rule` на:

```latex
Пусть \( X \) "--- множество допустимых текстовых сообщений, представленных
после нормализации в~виде канонической строки \texttt{normalized\_text}, и~пусть
\( Y_{\text{user}} = \{y_1, \dots, y_K\} \) "--- множество пользовательских
классов, формируемое в~процессе инициализации системы. Классификатор обучается
только этим классам; сообщения, которые не~удаётся уверенно отнести ни~к~одному
из~них, не~приписываются принудительно, а~получают \emph{отказ} от~классификации,
обозначаемый \texttt{other}. Решающая функция тем самым определяется
на~расширенном множестве исходов
\begin{equation}
    \label{eq:label-set}
    \widetilde{Y} = Y_{\text{user}} \cup \{\,\texttt{other}\,\},
\end{equation}
где \texttt{other} "--- не~самостоятельный класс, а~исход «воздержаться
от~решения».

Задача классификации формулируется как~построение отображения
\begin{equation}
    \label{eq:classifier}
    f \colon X \to \widetilde{Y},
\end{equation}
реализуемого как~композиция векторизатора \( \varphi \colon X \to \mathbb{R}^{d} \),
параметризованного классификатора \( f_\theta \colon \mathbb{R}^{d} \to \mathbb{R}^{K} \),
возвращающего апостериорные оценки принадлежности \( p_k(x) \) для каждого
пользовательского класса (классификатор \( f_\theta \) обучается только
классам \( Y_{\text{user}} \) и~метку \texttt{other} не~предсказывает),
и~решающего правила, учитывающего порог уверенности \( \tau \in (0, 1) \):
\begin{equation}
    \label{eq:decision-rule}
    f(x) =
    \begin{cases}
        \arg\max\limits_{k \in \{1, \dots, K\}} p_k(x), & \text{если } \max_{k} p_k(x) \geqslant \tau, \\
        \texttt{other},                                 & \text{иначе (отказ).}
    \end{cases}
\end{equation}
Параметры \( f_\theta \) настраиваются минимизацией эмпирического риска
на~размеченной выборке \( \{(x_i, y_i)\}_{i=1}^{N} \subseteq X \times Y_{\text{user}} \):
\begin{equation}
    \label{eq:empirical-risk}
    \widehat{R}(f_\theta) = \frac{1}{N} \sum_{i=1}^{N} \mathbb{1}\bigl[\,\hat{y}(x_i) \neq y_i\,\bigr],
    \qquad \hat{y}(x) = \arg\max_{k} p_k(x).
\end{equation}
Отказ \texttt{other} не~входит в~обучающую разметку и~не~участвует в~этой
величине: он определяется лишь порогом \( \tau \) на~этапе применения
правила~\eqref{eq:decision-rule}.
```

Примечание: порядок уравнений изменён (label-set → classifier → decision-rule → empirical-risk). Все четыре label сохранены, поэтому `\eqref` в остальной главе остаются валидными.

- [ ] **Step 2: Зафиксировать в памяти, что правок сборки тут не проверяем — отложено до Task A6 BUILD.**

(Микро-шаг: ничего не запускать, продолжить редактирование §1.2.)

### Task A2: Две роли отказа (§1.2.1, строки 102-111)

**Files:**
- Modify: `Dissertation/part1.tex:102-111`

- [ ] **Step 1: Заменить абзац о «двух функциональных предназначениях».**

Заменить текст от «Категория \texttt{other} имеет два функциональных…» до «…рассматривается в~главе~\ref{ch:ch2}.» на:

```latex
Отказ \texttt{other} выполняет две роли. Первая "--- безопасное поведение
при~низкой уверенности: при~\( \max_k p_k(x) < \tau \) система воздерживается
от~решения, что предпочтительнее ошибочного отнесения сообщения к~постороннему
классу. Вторая "--- исходы-отказы образуют отдельный поток сообщений, требующих
дальнейшего рассмотрения. В~общем случае правило~\eqref{eq:decision-rule}
допускает покомпонентные пороги \( \tau_k \) и~дополнительную проверку
принадлежности к~области известных данных (out-of-distribution) по~расстоянию
до~ближайшего центра кластера или по~калиброванной уверенности.
```

(Удалены: «переаннотации и переобучения модели» и отсылка «детально рассматривается в~главе~2» — проектное и форвард-реф, по решениям 12/13.)

### Task A3: §1.2.2 — заголовки и формулировка отказа

**Files:**
- Modify: `Dissertation/part1.tex:130-160`

- [ ] **Step 1: Русифицировать псевдозаголовки и переписать пункт об отказе.**

- Строка 136: `\textit{Bootstrap классов.}` → `\textit{Начальное формирование классов.}`
- Строка 142-148: пункт `\textit{Обязательная категория \texttt{other}.}` заменить на:

```latex
\textit{Обязательный отказ от~классификации.} Любой содержательный поток
включает сообщения, не~относящиеся к~устоявшимся классам пользователя: разовая
переписка, технические уведомления нового типа, смена тематической структуры
архива. Возможность отказа \texttt{other} в~решающем правиле~\eqref{eq:decision-rule}
исключает принудительное распределение таких сообщений по~посторонним классам.
```

- Строка 150: `\textit{Locality и приватность.}` → `\textit{Локальность и~приватность.}`
- Строка 156: `\textit{Multilingual-характер.}` → `\textit{Многоязычность потока.}`
- В теле этих пунктов: `Multilingual` / `multilingual` → «многоязычный/многоязычность» (см. словарь Task D1).

### Task A4: §1.2.3 — метрики только по `Y_user`, `other` не измеряется

**Files:**
- Modify: `Dissertation/part1.tex:162-208`

- [ ] **Step 1: Добавить оговорку об области применения метрик.**

После предложения, заканчивающегося «…дополненный показателями, характерными для постановки с~категорией \texttt{other}.» (строка 167), добавить:

```latex
Все метрики качества классификации вычисляются только по~пользовательским
классам \( Y_{\text{user}} \); отказ \texttt{other} не~является классом
и~отдельной метрикой качества в~работе не~оценивается.
```

- [ ] **Step 2: Переформулировать блок coverage/other-rate как операционный (строки 194-208).**

Заменить абзац «Помимо классических метрик фиксируются два показателя…» на:

```latex
Помимо классических метрик фиксируются два операционных показателя, описывающих
объём отказов. Покрытие (coverage) "--- доля сообщений, для которых классификатор
принял решение в~пользу одного из~пользовательских классов:
\begin{equation}
    \label{eq:coverage}
    \mathrm{Coverage} = \frac{1}{N_{\mathrm{test}}}\sum_{i=1}^{N_{\mathrm{test}}} \mathbb{1}\bigl[\,f(x_i) \neq \texttt{other}\,\bigr].
\end{equation}
Доля отказов определяется как~\( 1 - \mathrm{Coverage} \). Оба показателя
характеризуют объём, а~не~качество отказа, и~рассматриваются совместно
с~macro-F1: при~фиксированном уровне ошибок более высокое покрытие означает
большую практическую применимость системы. Дополнительно анализируется
матрица ошибок, позволяющая выявлять систематические смешения пользовательских
классов.
```

### Task A5: §1.2.4 — убрать проектные решения, перефразировать ссылку на формулу

**Files:**
- Modify: `Dissertation/part1.tex:210-233`

- [ ] **Step 1: Перефразировать абзац о дисбалансе (строки 212-219).**

Заменить на:

```latex
Распределение пользовательских классов в~почтовом архиве заведомо
неравномерное: несколько активных проектов формируют большую часть переписки,
тогда как~ряд категорий представлен единичными примерами. Из~показателей
агрегированной оценки~\eqref{eq:macro-f1} accuracy в~таких условиях маскирует
деградацию качества на~редких классах, тогда как~macro-F1 усредняет качество
по~классам равновесно; поэтому основной метрикой сравнения моделей в~работе
принят macro-F1.
```

(Удалено: «На~уровне обучения дисбаланс компенсируется взвешиванием функции потерь…» — проектное решение, решение 12. Двойная ссылка на `eq:macro-f1` устранена — решение 9.)

- [ ] **Step 2: Переписать абзац о concept drift как теоретический (строки 221-233).**

Заменить на:

```latex
Свойства потока писем меняются со~временем: появляются новые проекты
и~отправители, меняются форматы автоматических уведомлений, отдельные классы
теряют активность. Это явление известно как~дрейф концепций (concept drift)
\autocite{Gama2014ConceptDrift} и~характерно для рассматриваемой задачи;
в~смежной постановке оно представлено в~подходе EmFORE, реализующем явное
обновление правил классификации при~изменении распределения признаков
\autocite{Singh2023EmFORE}. Учёт дрейфа требует механизма обнаружения
расхождения активной модели с~текущим распределением писем и~последующего
обновления модели; соответствующие требования к~системе формулируются
в~разделе~\ref{sec:ch1/dev}.
```

(Удалены конкретные механизмы переобучения и quality gates с отсылкой в главу 2; ссылка переведена на внутренний §1.7.)

### Task A6: BUILD — проверка после Фазы A

**Files:**
- Verify: сборка

- [ ] **Step 1: Собрать черновик.**

Run: `make dissertation-draft`
Expected: сборка завершается без ошибок; в `.log` нет `LaTeX Warning: Reference ... undefined` для `eq:label-set`, `eq:classifier`, `eq:decision-rule`, `eq:empirical-risk`, `eq:coverage`, `eq:macro-f1`.

- [ ] **Step 2: Commit.**

```bash
git add Dissertation/part1.tex
git commit -m "Глава 1: other как отказ от классификации, чистка формализма §1.2"
```

---

## Фаза B. Теоретичность и самодостаточность (§1.3–1.5)

### Task B1: §1.3 — самодостаточный вывод вместо форвард-рефа

**Files:**
- Modify: `Dissertation/part1.tex:285-292`

- [ ] **Step 1: Переписать «Сводную типологию» (§1.3.4), убрав отсылку в главу 2.**

Заменить абзац «Описанные семейства образуют последовательность…» (строки 285-292) на:

```latex
Описанные семейства образуют последовательность с~монотонно растущей
выразительной способностью и~монотонно усложняющимися условиями обучения
и~исполнения (рисунок~\ref{fig:embedding-taxonomy}). Счётные и~статичные
представления остаются полезной базовой линией и~используются в~работе
как~контрольная точка, однако только последняя группа методов одновременно
отвечает требованию многоязычности и~не~требует переобучения с~нуля
на~локальном архиве писем; поэтому в~качестве основного представления
рассматриваются контекстные многоязычные модели уровня предложения.
```

(Удалена отсылка «Вопросы локального исполнения… рассматриваются в~главе~2 при~описании компонента \texttt{normalizer}--\texttt{embedder}» — проектное + форвард-реф.)

### Task B2: §1.4 — самодостаточный вывод вместо форвард-рефа

**Files:**
- Modify: `Dissertation/part1.tex:357-363`

- [ ] **Step 1: Переписать заключительный `\medskip`-абзац §1.4.**

Заменить «Совместно перечисленные методы покрывают спектр…» (строки 359-363) на:

```latex
Совместно перечисленные методы покрывают спектр от~интерпретируемых линейных
моделей до~гибких нейронных архитектур с~калибровкой. С~точки зрения трёх
критериев раздела "--- совместимость с~ограниченной разметкой, поддержка
инкрементного обучения и~калибровка вероятностей "--- ни~одно семейство
не~доминирует: линейные и~онлайн-модели выигрывают по~стоимости обновления,
но~требуют отдельной калибровки уверенности; нейронная надстройка
над~многоязычными эмбеддингами даёт наиболее однородное представление
для~постановки с~отказом, но~чувствительна к~калибровке выходных вероятностей.
Эти наблюдения задают критерии выбора конкретной модели на~этапе проектирования.
```

(Удалена отсылка «рассматриваются в~главе~2 вместе с~решением для постобработки уверенности».)

### Task B3: §1.5 — добавить абзац-вывод (решение 10)

**Files:**
- Modify: `Dissertation/part1.tex:412-431`

- [ ] **Step 1: Заменить отсылку в главу 2 в §1.5.4 на внутреннюю.**

В абзаце §1.5.4 (строки 412-422) заменить хвост «…принимается в~процедуре review с~привлечением пользователя, описанной в~главе~\ref{ch:ch2}.» на:

```latex
…а~окончательное решение о~принятии кластеров требует ручной проверки
кандидатов с~привлечением пользователя.
```

- [ ] **Step 2: Добавить замыкающий абзац-вывод §1.5 после рисунка `fig:clustering-outliers` (после строки 431).**

```latex
По~двум требованиям раздела "--- работа без~априорного числа классов и~явное
выделение сообщений вне устойчивых кластеров "--- предпочтительны плотностные
методы: HDBSCAN определяет устойчивые кластеры переменной плотности и~помечает
остальные точки как~шум, структурно сопоставимый с~отказом \texttt{other}.
Для~работы в~пространстве контекстных эмбеддингов высокой размерности
плотностная кластеризация дополняется предварительным нелинейным снижением
размерности (UMAP), тогда как~партиционный \( k \)-means сохраняется
как~базовая линия.
```

### Task B4: BUILD — проверка после Фазы B

**Files:**
- Verify: сборка

- [ ] **Step 1: Собрать черновик.**

Run: `make dissertation-draft`
Expected: без ошибок; число неразрешённых `\ref{ch:ch2}` уменьшилось (часть форвард-рефов снята), `fig:embedding-taxonomy` и `fig:clustering-outliers` разрешаются.

- [ ] **Step 2: Commit.**

```bash
git add Dissertation/part1.tex
git commit -m "Глава 1: теоретические выводы §1.3-1.5, снятие проектных отсылок"
```

---

## Фаза C. Сравнительный анализ систем (§1.6)

### Task C1: Исправить таблицу сравнения (решение 5)

**Files:**
- Modify: `Dissertation/part1.tex:499-518`

- [ ] **Step 1: Изменить спорные ячейки в строках Focused/Priority Inbox.**

В строке `Focused Inbox` (строки 499-500): `Multilingual` `\(\pm\)` → `н/д`; `Concept drift` `\(+\)` → `\(\pm\)`.
В строке `Priority Inbox` (строки 502-503): `Multilingual` `\(\pm\)` → `н/д`; `Concept drift` `\(+\)` → `\(\pm\)`.

Итоговые строки таблицы:

```latex
            Focused Inbox~\autocite{Microsoft2024FocusedInbox}
                                         & \(-\)\tnote{3}               & н/д          & \(-\)        & \(-\)                   & \(\pm\)       & встроенная бинарная \\
            \hline
            Priority Inbox~\autocite{Aberdeen2010PriorityInbox}
                                         & \(-\)\tnote{3}               & н/д          & \(-\)        & \(-\)                   & \(\pm\)       & встроенная бинарная \\
```

- [ ] **Step 2: Добавить сноску, поясняющую `\(\pm\)` для дрейфа.**

В `tablenotes` (строки 514-518) добавить четвёртый пункт и сослаться на него у `\(\pm\)`-ячеек через `\tnote{4}`:

```latex
            \item[4] Адаптация по~неявным сигналам пользователя; явный механизм учёта дрейфа концепций отсутствует.
```

(Поставить `\tnote{4}` рядом с `\(\pm\)` в обеих строках Concept drift.)

### Task C2: Кратко обосновать оценки в тексте (решение 6)

**Files:**
- Modify: `Dissertation/part1.tex:441-480`

- [ ] **Step 1: §1.6.1 (Focused Inbox) — добавить предложение об адаптации/многоязычности.**

В конец §1.6.1 (после строки 449) добавить:

```latex
Многоязычность системы в~документации не~специфицирована, а~обновление модели
по~сигналам чтения и~переноса писем реализует адаптацию, но~не~декларируется
как~явный механизм учёта дрейфа концепций.
```

- [ ] **Step 2: §1.6.2 (Priority Inbox) — добавить аналогичное предложение.**

В конец §1.6.2 (после строки 459) добавить:

```latex
Как~и~в~Focused~Inbox, многоязычное поведение отдельно не~описано, а~онлайн-
дообучение обеспечивает адаптацию к~пользователю без~явной модели дрейфа
концепций.
```

### Task C3: BUILD — проверка после Фазы C

**Files:**
- Verify: сборка

- [ ] **Step 1: Собрать черновик.**

Run: `make dissertation-draft`
Expected: таблица `tab:systems-compare` компилируется, сноски `\tnote{4}` разрешаются, нет ошибок выравнивания колонок.

- [ ] **Step 2: Commit.**

```bash
git add Dissertation/part1.tex
git commit -m "Глава 1: согласование таблицы сравнения систем с текстом §1.6"
```

---

## Фаза D. Терминология и стиль (вся глава)

### Task D1: Сквозная русификация англицизмов (решение 4)

**Files:**
- Modify: `Dissertation/part1.tex` (вся глава)

**Словарь замен** (русский термин; при первом упоминании в скобках оригинал, далее без скобок). Обозначение-идентификатор `\texttt{other}` оставляем латиницей единообразно как термин-метку:

| Оригинал | Замена |
|---|---|
| multilingual / Multilingual | многоязычный / многоязычность (multilingual) |
| locality / Locality | локальность исполнения (locality) |
| bootstrap / Bootstrap | начальное формирование классов (bootstrap) |
| concept drift | дрейф концепций (concept drift) |
| coverage / Coverage | покрытие (coverage) |
| other-rate | доля отказов |
| review | ручная проверка |
| snapshot / training snapshot / training dataset snapshot | обучающий срез |
| quality gates | критерии активации (quality gates) |
| OOV | слова вне словаря (OOV) |
| «importance / not important», «important/other» | русские формулировки в кавычках-«ёлочках» |

- [ ] **Step 1: Найти все вхождения латиницы для контроля охвата.**

Run: `grep -noE 'multilingual|Multilingual|locality|Locality|bootstrap|Bootstrap|concept drift|coverage|Coverage|other-rate|snapshot|quality gates|OOV|review' Dissertation/part1.tex`
Expected: список строк; каждую обработать по словарю. Учитывать, что `Coverage` в `\mathrm{Coverage}` (формула) НЕ трогаем — это математическое обозначение; в окружающем тексте — «покрытие (coverage)» при первом упоминании.

- [ ] **Step 2: Применить замены по словарю, вводя скобочный оригинал только при первом появлении термина в главе.**

Точечно: §1.1 строки 41/156 (multilingual), 44 (локальная обработка — уже рус.), 47 (concept drift), §1.2 (multilingual-характер уже сделан в A3), §1.6 строки 525 (multilingual), и т.д. После замен — проверить, что термин «дрейф концепций (concept drift)» вводится со скобкой ровно один раз (в §1.2.4, Task A5 Step 2), а дальше без скобок; убрать разнобой «дрейф категорий» (строка 56) → «дрейф концепций».

### Task D2: Опечатки и единообразие (решения 3, 11)

**Files:**
- Modify: `Dissertation/part1.tex`

- [ ] **Step 1: Исправить опечатку OOD.**

Строка 109 (если ещё присутствует после Task A2 — там она уже переписана на «out-of-distribution»; проверить остаток главы):
Run: `grep -n 'on-of-distribution' Dissertation/part1.tex`
Expected: пусто. Если найдено — заменить на `out-of-distribution`.

- [ ] **Step 2: Унифицировать обозначение macro-F1.**

Выбрать единый вид «macro-F1» в тексте (вне формул) и применить. В формулах оставить `\mathrm{macro\text{-}F_1}`.
Run: `grep -noE 'macro-F1|macro\\text' Dissertation/part1.tex` — свериться, что в прозе везде «macro-F1».

- [ ] **Step 3: Мелкие правки.**

- Строка 330: `дешевы` → `вычислительно дёшевы`.
- Строки 28-35 (одно длинное предложение): разбить на два по границе «Встроенные автоматические механизмы…».
Run: `grep -n 'дешевы' Dissertation/part1.tex`
Expected после правки: пусто.

### Task D3: BUILD — проверка после Фазы D

**Files:**
- Verify: сборка

- [ ] **Step 1: Собрать черновик.**

Run: `make dissertation-draft`
Expected: без ошибок.

- [ ] **Step 2: Контроль остаточной латиницы.**

Run: `grep -noE 'multilingual|locality|bootstrap|coverage|snapshot|quality gates' Dissertation/part1.tex`
Expected: вхождения только внутри скобок «(…)» как пояснение к русскому термину, либо отсутствуют.

- [ ] **Step 3: Commit.**

```bash
git add Dissertation/part1.tex
git commit -m "Глава 1: русификация терминологии и единообразие обозначений"
```

---

## Фаза E. Введение под содержание глав (решение 8)

### Task E1: Переписать список задач во введении

**Files:**
- Modify: `common/characteristic.tex:68-92`

- [ ] **Step 1: Заменить блок задач, согласовав с §1.7.2 главы 1.**

Заменить содержимое `enumerate` (строки 69-92) на:

```latex
  \item проанализировать существующие подходы к~классификации текстовых
        документов и~автоматической организации электронной почты
        и~сформулировать формальную постановку задачи с~явным отказом
        от~классификации;
  \item реализовать нормализацию входящих сообщений в~канонический контракт
        \texttt{normalized\_text} с~выделением полей заголовка, очищенного
        текста и~идентификатора языка;
  \item разработать локальный многоязычный (multilingual) векторизатор
        русско-английской переписки, не~требующий передачи текста писем
        во~внешние сервисы;
  \item разработать процедуру начального формирования пользовательских классов
        кластеризацией исторической выборки в~признаковом пространстве
        с~явным выделением выбросов и~подготовкой кандидатов для~ручной проверки;
  \item реализовать классификатор, отдающий калиброванные апостериорные оценки
        принадлежности, и~решающее правило с~порогом уверенности и~отказом
        \texttt{other} для~низкоуверенных сообщений;
  \item реализовать цикл обратной связи и~версионирование моделей с~активацией
        новой версии по~критериям качества (macro-F1, взвешенный F1, покрытие);
  \item провести экспериментальное исследование на~репрезентативном архиве
        писем с~отчётом по~метрикам, введённым в~первой главе.
```

### Task E2: Согласовать новизну и методы с переформулированным `other`

**Files:**
- Modify: `common/characteristic.tex:103-131`

- [ ] **Step 1: Методы — русифицировать coverage (строки 103-111).**

Строка 108: `coverage, доля сообщений в~категории \texttt{other}` → `покрытие (coverage), доля отказов в~категорию \texttt{other}`.

- [ ] **Step 2: Новизна, пункт 3 (строки 123-126) — переформулировать как отказ.**

Заменить на:

```latex
  \item Предложена схема принятия решений с~явным отказом от~классификации
        (\texttt{other}), при~которой низкоуверенные и~потенциально
        out-of-distribution сообщения не~приписываются к~классам, а~сохраняются
        как~материал для~обратной связи и~последующего переобучения.
```

- [ ] **Step 3: Терминология новизны (строка 121).**

`с~выделением outliers` → `с~выделением выбросов (outliers)`.

### Task E3: BUILD — финальная проверка

**Files:**
- Verify: сборка диссертации (и, по возможности, автореферата, т.к. `characteristic.tex` общий)

- [ ] **Step 1: Полная черновая сборка.**

Run: `make dissertation-draft`
Expected: без ошибок; введение и глава 1 терминологически согласованы.

- [ ] **Step 2: Сборка автореферата (характеристика общая по ГОСТ).**

Run: `make synopsis-draft`
Expected: без ошибок (правки `characteristic.tex` корректно отражаются в автореферате).

- [ ] **Step 3: Commit.**

```bash
git add common/characteristic.tex
git commit -m "Введение: список задач и новизна согласованы с содержанием глав"
```

---

## Вне области плана

- **Решение 14:** мостик «текстовые документы → электронная почта» (§1.1, строки 16-21) — НЕ изменяется.
- **§1.7** «Постановка задачи разработки» сохраняется как формулировка требований (не проектное решение); из неё лишь не добавляются новые реализационные детали. Если потребуется убрать §1.7 целиком — это отдельное решение, в текущий план не входит.

---

## Self-Review

**Покрытие решений 1–14:** 1→A1/A2/A3; 2→A1(p_k); 3→D2; 4→A3/D1; 5→C1; 6→C2; 7→A4; 8→E1/E2; 9→A5; 10→B3; 11→D2; 12→A2/A5/B1/B2; 13→A2/B1/B2/B3 (снятие форвард-рефов); 14→«Вне области плана». Все 14 адресованы.

**Согласованность обозначений:** `\widetilde{Y}` (расширенное множество исходов) введено в A1 и больше нигде не конфликтует; `Y_{\text{user}}` используется единообразно; label-метки уравнений (`eq:label-set`, `eq:classifier`, `eq:decision-rule`, `eq:empirical-risk`, `eq:coverage`) сохранены — существующие `\eqref` в §1.2–1.7 остаются валидными. `\texttt{other}` зафиксирован как термин-метка (латиница) во всех фазах.

**Риск:** замена `y_{\text{other}}` на `\texttt{other}` в `eq:decision-rule`/`eq:coverage` — проверить, что нигде в главе не остаётся `y_{\text{other}}` (Task D2 grep-контроль рекомендуется расширить: `grep -n 'y_{\\text{other}}' Dissertation/part1.tex` → должно быть пусто после A1/A4).
